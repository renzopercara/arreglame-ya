/**
 * ServiceRequestStatus Enum
 * Lifecycle states for a marketplace service request
 */
export enum ServiceRequestStatus {
  CREATED = 'CREATED',
  OPEN = 'OPEN',
  ASSIGNED = 'ASSIGNED',
  COMPLETED = 'COMPLETED',
  CANCELLED = 'CANCELLED',
}
