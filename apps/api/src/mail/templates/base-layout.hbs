<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Arreglame Ya</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen', 'Ubuntu', 'Cantarell', 'Fira Sans', 'Droid Sans', 'Helvetica Neue', sans-serif;
      line-height: 1.6;
      color: #333;
      background-color: #f5f5f5;
    }

    .container {
      max-width: 600px;
      margin: 0 auto;
      background-color: #fff;
      border-radius: 8px;
      overflow: hidden;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }

    /* HEADER */
    .header {
      background: linear-gradient(135deg, #2563eb 0%, #7c3aed 100%);
      color: white;
      padding: 40px 20px;
      text-align: center;
    }

    .header-logo {
      font-size: 28px;
      font-weight: bold;
      margin-bottom: 10px;
    }

    .header-subtitle {
      font-size: 14px;
      opacity: 0.9;
    }

    /* CONTENT */
    .content {
      padding: 40px 30px;
    }

    .content h1 {
      font-size: 24px;
      color: #1f2937;
      margin-bottom: 20px;
    }

    .content p {
      font-size: 14px;
      line-height: 1.8;
      color: #4b5563;
      margin-bottom: 15px;
    }

    .content a {
      color: #2563eb;
      text-decoration: none;
      font-weight: 500;
    }

    .content a:hover {
      text-decoration: underline;
    }

    /* CTA BUTTON */
    .cta-button {
      display: inline-block;
      background: linear-gradient(135deg, #2563eb 0%, #7c3aed 100%);
      color: white;
      padding: 12px 32px;
      border-radius: 6px;
      text-decoration: none;
      font-weight: 600;
      font-size: 14px;
      margin: 20px 0;
      transition: transform 0.2s;
    }

    .cta-button:hover {
      transform: scale(1.02);
    }

    .text-center {
      text-align: center;
    }

    /* SECTION */
    .section {
      margin-bottom: 30px;
    }

    .section-title {
      font-size: 18px;
      color: #1f2937;
      margin-bottom: 15px;
      font-weight: 600;
    }

    /* HIGHLIGHT BOX */
    .highlight {
      background-color: #f3f4f6;
      border-left: 4px solid #2563eb;
      padding: 15px 20px;
      margin: 20px 0;
      border-radius: 4px;
    }

    .highlight p {
      margin: 0;
      font-size: 13px;
      color: #374151;
    }

    /* FEATURES LIST */
    .features {
      list-style: none;
      margin: 20px 0;
    }

    .features li {
      padding: 10px 0;
      padding-left: 25px;
      position: relative;
      font-size: 14px;
      color: #4b5563;
    }

    .features li:before {
      content: "✓";
      position: absolute;
      left: 0;
      color: #10b981;
      font-weight: bold;
      font-size: 16px;
    }

    /* FOOTER */
    .footer {
      background-color: #f9fafb;
      padding: 30px;
      text-align: center;
      border-top: 1px solid #e5e7eb;
    }

    .footer-links {
      margin-bottom: 15px;
    }

    .footer-links a {
      color: #6b7280;
      text-decoration: none;
      font-size: 12px;
      margin: 0 10px;
    }

    .footer-links a:hover {
      color: #2563eb;
    }

    .footer-text {
      font-size: 12px;
      color: #9ca3af;
      line-height: 1.6;
    }

    /* SOCIAL LINKS */
    .social-links {
      margin: 15px 0;
    }

    .social-links a {
      display: inline-block;
      width: 36px;
      height: 36px;
      background-color: #e5e7eb;
      border-radius: 50%;
      line-height: 36px;
      text-align: center;
      margin: 0 5px;
      color: #2563eb;
      font-weight: bold;
      text-decoration: none;
      transition: background-color 0.2s;
    }

    .social-links a:hover {
      background-color: #2563eb;
      color: white;
    }

    /* RESPONSIVE */
    @media (max-width: 600px) {
      .container {
        border-radius: 0;
      }

      .content {
        padding: 20px 15px;
      }

      .content h1 {
        font-size: 20px;
      }

      .header {
        padding: 30px 15px;
      }

      .footer {
        padding: 20px 15px;
      }
    }

    /* DIVIDER */
    .divider {
      border-top: 1px solid #e5e7eb;
      margin: 30px 0;
    }

    /* BADGE */
    .badge {
      display: inline-block;
      background-color: #dbeafe;
      color: #1e40af;
      padding: 4px 8px;
      border-radius: 4px;
      font-size: 12px;
      font-weight: 600;
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- HEADER -->
    <div class="header">
      <div class="header-logo">🔧 Arreglame Ya</div>
      <div class="header-subtitle">Conectando servicios y soluciones</div>
    </div>

    <!-- CONTENT -->
    <div class="content">
      {{{body}}}
    </div>

    <!-- FOOTER -->
    <div class="footer">
      <div class="footer-links">
        <a href="{{appUrl}}">Inicio</a>
        <a href="{{appUrl}}/faq">Preguntas Frecuentes</a>
        <a href="{{appUrl}}/contacto">Contacto</a>
        <a href="{{appUrl}}/privacidad">Privacidad</a>
      </div>

      <div class="social-links">
        <a href="https://facebook.com/arreglamya" title="Facebook">f</a>
        <a href="https://instagram.com/arreglamya" title="Instagram">📷</a>
        <a href="https://twitter.com/arreglamya" title="Twitter">𝕏</a>
      </div>

      <div class="footer-text">
        <p>© {{year}} Arreglame Ya. Todos los derechos reservados.</p>
        <p>Este es un mensaje automático. Por favor no responda a este correo.</p>
        <p>Si tienes problemas, <a href="{{appUrl}}/soporte">contáctanos aquí</a>.</p>
      </div>
    </div>
  </div>
</body>
</html>
